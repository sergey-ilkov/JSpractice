git init

git config --local user.name "Sergey"

git config --local user.emal ilkov1103@gmail.com

git add -A / git add *

git commit -a -m "commit 1"

git push -u название репозитория / ключ -u чтоб запомнить по умолчанию куда пушить наши файлы